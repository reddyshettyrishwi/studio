'use client';
import {
  useFirebase,
  useFirebaseApp,
  useFirestore,
  useAuth,
} from './provider';

// Re-exporting the hooks for easy import
export { useFirebase, useFirebaseApp, useFirestore, useAuth };
