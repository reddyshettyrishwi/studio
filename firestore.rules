
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }
    
    function getUserData() {
      return get(/databases/$(database)/documents/users/$(request.auth.uid)).data;
    }

    function isAdmin() {
      return isSignedIn() && getUserData().role == 'Admin';
    }

    match /users/{userId} {
      // A user can read their own profile.
      allow get: if isOwner(userId);
      
      // An admin can read any user's profile.
      allow get: if isAdmin();

      // Admins can list users, but ONLY if they are specifically querying for "Pending" users.
      // This is critical for the Approvals page to work securely.
      allow list: if isAdmin() && request.query.where.get('status')[2] == 'Pending';

      // A user can create their own document upon sign-up.
      allow create: if isOwner(userId);

      // An admin can update a user's status to approve or reject them.
      allow update: if isAdmin();
                       
      // A user can delete their own account. An admin can also delete accounts.
      allow delete: if isOwner(userId) || isAdmin();
    }

    /**
     * @description Allows any signed-in user to manage agencies.
     */
    match /agencies/{agencyId} {
      allow read, write: if isSignedIn();
    }

    /**
     * @description Allows any signed-in user to manage influencer profiles.
     */
    match /influencers/{influencerId} {
      allow read, write: if isSignedIn();
    }

    /**
     * @description Allows any signed-in user to manage campaigns.
     */
    match /campaigns/{campaignId} {
      allow read, write: if isSignedIn();
    }
  }
}
